<div id="book_review_form" class="bib_form">
<%= simple_form_for @bib do |form| %>

    <h3>Book Review</h3>

    <% if @bib.errors.any? %>
        <div id="error_explanation">
        <h2>
            <%= pluralize(@bib.errors.count, "error") %> prohibited
            this article from being saved:
        </h2>
        <ul>
            <% @bib.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
            <% end %>
        </ul>
        </div>
    <% end %>

    <%= form.input :reference_type, 
        :as => :hidden, 
        :input_html => { :value => "Book Review" } 
    %>

    <div id='bibliography_author_of_review'>
        <%= form.simple_fields_for :author_of_reviews do |c| %>
            <%= c.input :display_name, 
                label: 'Author of review',
                as: :string
            %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Author of Review', 
            form, 
            :author_of_reviews, 
            association: 'author_of_reviews', 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_author_of_review', 
                'association-insertion-method' => 'append'
            }, 
            partial: 'bibliography_citation_fields' 
        %>
    </div>
    
    <%= form.input :year_published, 
        label: 'Year', 
        as: :string
    %>

    <%= form.input :title_of_review, 
        label: 'Title of Review', 
        as: :string
    %>

    <%= form.input :journal_title,
        label: 'Journal title', 
        as: :string
    %>

    <%= form.input :volume, 
        label: 'Volume',
        as: :string
    %>

    <%= form.input :issue, 
        label: 'Issue',
        as: :string
    %>

    <%= form.input :page_range, 
        label: 'Page range',
        as: :string 
    %>

    <h4>Reviewed Author/Title</h4>
    <div id='bibliography_reviewed_component'>
        <ol id="bibliography_reviewed_component_ol">
            <%= form.simple_fields_for :reviewed_components do |rc| %>
                <%= render 'reviewed_components_fields', :form => rc %></li>
            <% end %>
        </ol>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Reviewed Author/Title', 
            form, 
            :reviewed_components, 
            association: 'reviewed_components', 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_reviewed_component_ol', 
                'association-insertion-method' => 'append'
            }, 
            partial: 'reviewed_components_fields' 
        %>
    </div>

    <%= form.input :epub_date, 
        label: 'Epub date',
        as: :string
    %>

    <%= form.input :date,
        label: 'Date', 
        as: :string
    %>

    <h4>ISSN</h4>
    <div id='bibliography_issn'>
        <%= form.simple_fields_for :issns do |c| %>
            <%= c.input :value, 
                label: 'ISSN', 
                as: :string
            %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add ISSN', 
            form, 
            :issns, 
            association: 'issns', 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_issn', 
                'association-insertion-method' => 'append'
            }, 
            partial: 'bibliography_standard_identifier_fields' 
        %>
    </div>

    <div id='bibliography_doi'>
        <%= form.simple_fields_for :dois do |c| %>
            <%= c.input :value, 
                label: 'DOI', 
                as: :string
            %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add DOI', 
            form, 
            :dois, 
            association: 'dois', 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_doi', 
                'association-insertion-method' => 'append'
            }, 
            partial: 'bibliography_standard_identifier_fields' 
        %>
    </div>

    <%= form.input :worldcat_url, 
        label: 'Worldcat URL',
        as: :string
    %>

    <%= form.input :publisher_url, 
        label: 'Publisher URL',
        as: :string
    %>

    <%= form.input :leuven_url, 
        label: 'Leuven URL',
        as: :string
    %>

    <h4>Subjects</h4>
    <div id='bibliography_subjects'>
        <%= form.simple_fields_for :bibliography_subjects do |subject| %>
            <%= render 'bibliography_subject_fields', :form => subject %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Subject', 
            form, 
            :bibliography_subjects, 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_subjects', 
                'association-insertion-method' => 'append'
            } 
        %>
    </div>

    <h4>Periods</h4>
    <div id='bibliography_periods'>
        <%= form.simple_fields_for :bibliography_periods do |period| %>
            <%= render 'bibliography_period_fields', :form => period %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Period', 
            form, 
            :bibliography_periods, 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_periods', 
                'association-insertion-method' => 'append'
            } 
        %>
    </div>

    <h4>Locations</h4>
    <div id='bibliography_locations'>
        <%= form.simple_fields_for :bibliography_locations do |location| %>
            <%= render 'bibliography_location_fields', :form => location %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Location', 
            form, 
            :bibliography_locations, 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_locations', 
                'association-insertion-method' => 'append'
            } 
        %>
    </div>

    <h4>Entities</h4>
    <div id='bibliography_entities'>
        <%= form.simple_fields_for :bibliography_entities do |entity| %>
        <%= render 'bibliography_entity_fields', :form => entity %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Entity', 
            form, 
            :bibliography_entities, 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_entities', 
                'association-insertion-method' => 'append'
            } 
        %>
    </div>

    <%= form.input :abstract, 
        label: 'Abstract'
    %>

    <div id='bibliography_translated_author'>
        <%= form.simple_fields_for :translated_authors do |c| %>
            <%= c.input :display_name, 
                label: 'Translated author',
                as: :string
            %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Translated Author', 
            form, 
            :translated_authors, 
            association: 'translated_authors', 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_translated_author', 
                'association-insertion-method' => 'append'
            }, 
            partial: 'bibliography_citation_fields' 
        %>
    </div>

    <%= form.input :translated_title, 
        label: 'Translated Title',
        as: :string
    %>

    <div id='bibliography_languages'>
        <%= form.simple_fields_for :languages do |language| %>
            <%= language.input :name, 
                label: 'Language name',
                as: :string
            %>
        <% end %>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Language', 
            form, 
            :languages,
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_languages', 
                'association-insertion-method' => 'append'
            },
            partial: 'languages_fields'
        %>
    </div>

    <h4>Comments</h4>
    <div id="bibliography_comments">
        <ol id="bibliography_comments_ol">
        <%= form.simple_fields_for :comments do |comment| %>
            <%= render 'comment_fields', :form => comment %></li>
        <% end %>
        </ol>
    </div>
    <div class='links'>
        <%= link_to_add_association 'Add Comment', 
            form, 
            :comments, 
            class: 'btn btn-warning', 
            form_name: 'form', 
            data: {
                'association-insertion-node' => '#bibliography_comments_ol', 
                'association-insertion-method' => 'append'
            } 
        %>
    </div>

    <br/>
    <%= form.button :submit, "Save Bibliography", class: "btn btn-primary" %>
<% end %>
</div>
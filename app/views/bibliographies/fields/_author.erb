<div id='bibliography_authors' class='repeatable-field'>
    <div class="fields">
        <%= form.simple_fields_for :authors do |peep| %>
            <%= peep.association :person, 
                label: field_label,
                collection: Person.order(:name),
                prompt: 'Choose an Author',
                form_name: 'peep',
                input_html: {class: 'authors'}
            %>
        <% end %>
    </div>
    <div class='links clearfix'>
        <div class="col-sm-2"></div>
        <div class="col-sm-10 btn-add-container">
            <%= link_to_add_association 'Add ' + field_label, 
                form, 
                :authors, 
                class: 'btn btn-warning btn-add', 
                form_name: 'form', 
                data: {
                    'association-insertion-node' => '#bibliography_authors .fields', 
                    'association-insertion-method' => 'append'
                },
                partial: 'person_fields',
                render_options:  {
                    locals: { 
                        prompt_label: 'Choose an Author', 
                        class_name: 'authors' 
                    }
                }
            %>
        </div>
    </div>
</div>

<script>
$(document).ready(function() {
    $('.authors').select2();
    $("#bibliography_authors").on('cocoon:after-insert', function(e, insertedItem){
        $('.authors').last().select2();
    });
});
</script>
<% required_status = required_status ||= false %>
<% pretend_required = pretend_required ||= false %>
<div id='publisher_urls' class='repeatable-field'>
    <div class="row string optional bibliography-publisher_urls-fields">
        <label class="col-sm-2 col-form-label string optional"><%= field_label %></label>
        <div class="col-sm-10">
            <div class="fields">
                <%= form.simple_fields_for :publisher_urls do |url| %>
                    <%= render 'url_fields', 
                        form: url
                    %>
                <% end %>
            </div>
        </div>
    </div>
    <div class='links clearfix'>
        <div class="col-sm-2"></div>
        <div class="col-sm-10 btn-add-container">
            <%= link_to_add_association Bibliography::ADD_BUTTON + field_label, 
                form, 
                :publisher_urls,
                class: 'btn btn-warning btn-add fa-icon', 
                form_name: 'form', 
                data: {
                    'association-insertion-node' => '#publisher_urls .fields', 
                    'association-insertion-method' => 'append'
                }, 
                partial: 'url_fields' 
            %>
        </div>
    </div>
</div>
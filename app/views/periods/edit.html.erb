<% if @period.nil? %>
    <p>This record doesn't exist.</p>
    <%= link_to 'Go Back to Period index', periods_path %>
<% else %>
    <h1>Editing Period #<%= @period.id %></h1>

    <%= render 'form' %>
<% end %>
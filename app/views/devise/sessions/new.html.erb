<h2>Sign in</h2>

<div class="col-md-6 col-sm-12">
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions button-group">
      <%= f.button :submit, "Sign in", class: 'login-button btn btn-primary' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>

<div class="col-md-6 col-sm-12">
  <p>Please sign into your free account for the Jesuit Online Bibliography.
  While you do not need an account to use this database, a standard account allows you to email records 
  and to access bookmarked records and saved searches between visits.</p>

  <p>If you wish to volunteer as a <%= link_to 'contributor', about_page_path(:network) %> 
  and directly submit your records to the database, please email the <a href="mailto:jesuitonlinebibliography@bc.edu?subject=Question about becoming a correspondent">editors</a>. 
  Requests for contributor accounts are reviewed within 24 hours.</p>
</div>
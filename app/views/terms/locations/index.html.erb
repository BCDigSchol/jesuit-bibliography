<% @page_title = browse_page_title letter: @letter, browse_by: 'Locations' %>

<h2>Browse all Locations</h2>

<p>Below you can browse the locations examined in the project's records (the 'where' of the records). Each term appears in an alphabetical list, and when selected it will display all results associated with the specific term.</p>

<%= alphabetical_paginate @alpha_params %>

<div id="pagination_table" class="term-list-container">
  <% @locations.each do |loc| %>
    <div class="term-list-item"><a href="/catalog?f[locations_facet][]=<%= loc.name %>"><%= loc.name %></a></div>
  <% end %>
</div>
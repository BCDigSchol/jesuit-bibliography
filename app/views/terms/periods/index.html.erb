<h1>Browse all Centuries</h1>

<div id="pagination_table" class="term-list-container">
  <% @periods.each do |per| %>
    <div class="term-list-item"><a href="/?f[centuries_facet][]=<%= per.name %>"><%= per.name %></a></div>
  <% end %>
</div>